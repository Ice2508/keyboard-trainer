1. Создать функцию которая выводит в алерт, 
факториал числа до указанного(указанное число передается аргументом в функцию)
Факториал - произведение чисел.(решить циклом и рекурсивно)

2. Дан массив слов const words = ['apple', 'banana', 'apple']
Нужно создать объект где ключем будет слово, а значением колличество в массиве.
Пример const obj = {apple: 2, banana: 1}

3. Проверить является ли слово полиндром. Вывести алерт является или нет. Полиндром слово,
которое одинаково читается с двух сторон.

4. Найти самое большое число в массиве.

5. Удалить дубликаты из массива. 

6. Создать счетчик при помощи замыкания. Другие примеры замыкания.

7. Поиск уникального элемента в массиве.

8. Нахождение подмассива с максимальной суммой.

9. Максимальная подстрока без повторений. Описание задачи:
Напишите функцию для нахождения длины самой длинной подстроки 
без повторяющихся символов в строке.

10. Фибоначчи (n-й элемент)
В ответе: 0, 1, 1, 2, 3, 5, 8, 13 и т д.

11. Создаёт на странице пустой список <ul>.
Добавляет на страницу поле ввода <input> и кнопку <button> с текстом "Добавить".
При вводе текста в поле и клике на кнопку добавляет новый элемент <li> с введённым текстом в список.
Каждый элемент списка должен содержать кнопку "Удалить", которая удаляет этот элемент из списка при клике.

12. Асинхронный код: имитация запроса с использованием Promise и async/await
Задача:
Написать функцию, которая имитирует задержку (например, через setTimeout) и возвращает промис, 
резолвящийся через заданное время с каким-то значением.
Потом написать async-функцию, которая последовательно вызывает несколько таких задержек и выводит результат.

13. Глубокое копирование объекта
Задача:
Написать функцию, которая создаёт глубокую копию объекта (в том числе вложенных объектов и массивов), 
без использования JSON.parse(JSON.stringify(...)).

14. Поиск путей в лабиринте (Backtracking)
Задача:
Дан двумерный массив (матрица) из 0 и 1, где 0 — проход, а 1 — стена. 
Нужно написать функцию, которая найдет путь из верхнего левого угла (0,0) в нижний правый угол (n-1,m-1) 
и вернет последовательность координат этого пути (или null, если пути нет).

15. Пример простого запроса на апи. asunc await, then. 
https://catfact.ninja/fact возращаемый обект
16. Дана страница с заголовком страница1 и текстом в текст ариа, который можно менять 
так же есть кнопка перехода на другую страницу, нужно сохранить текст из текст ари при изменении в локал сторидж
и от туда брать, указав этот текст и на странице с заголовком страница 2. При обновлении как первой так и 
второй страницы, текст не должен меняться. 
17. Контекст this. Чему равен у обычной функции, чему у стрелочной? Показать на примерах, а так же показать
пример потери контекста. Что такое call, bind, apply; 
