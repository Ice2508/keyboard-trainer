1. 🔸 Что такое семантическая верстка?
Ответ: Использование HTML-тегов по их смыслу (например, <header>, <article>, <section>, <footer>), 
чтобы улучшить структуру, доступность и SEO страницы.

2. 🔸 Для чего нужен alt у изображений?
Ответ: Это текстовое описание изображения для:
людей с ограничениями по зрению (скринридеры),
ситуаций, когда изображение не загружается,
SEO.

3.🔸 Что такое meta charset="UTF-8"?
Ответ: Указывает кодировку страницы. 
UTF-8 поддерживает кириллицу, латиницу и почти все языки мира.

4.🔸 Что такое каскадность?
Ответ: Это принцип, по которому выбирается стиль, если на один элемент влияет несколько правил:
приоритет по специфичности,
порядок объявления,
!important.

5. 🔸 Как работает модель коробки (Box Model)?
Ответ: Любой элемент — это
margin → border → padding → content
Общий размер: width + padding + border + margin

6. 🔸 В чём разница между position: relative, absolute, fixed, sticky?
Ответ:
relative — сдвиг от обычного положения
absolute — позиционируется относительно ближайшего родителя с position (кроме static)
fixed — позиционируется относительно окна браузера
sticky — ведёт себя как relative, пока не достигнет порога — потом становится fixed

7. 🔸 Разница между var, let, const?
Ответ:
var — функциональная область видимости, поднимается
let/const — блочная область, не поднимаются (инициализация позже)
const нельзя переопределить (но можно мутировать объект)

8. 🔸 Что такое замыкание (closure)?
Ответ: Функция «помнит» переменные из внешней функции, даже после её завершения.

9. 🔸 Что такое callback и почему это важно?
Ответ: Callback — это функция, переданная как аргумент в другую функцию. 
Позволяет управлять асинхронностью и последовательностью действий.

10. Методы массива: push() - Добавляет в конец массива.
                    pop()  - Удаляет последний элемент.
                    unshift() - Добавляет в начало массива.
                    shift() - Удаляет первый элемент.
                    splice() - Добавляет/удаляет/заменяет элементы.
                    slice() - Возвращает копию части массива или строки
                    concat() - Объединяет массивы.
                    includes() - Проверяет наличие значения в массиве или строке.
                    indexOf() - Индекс первого вхождения элемента
                    find() - Возвращает первый элемент, подходящий по условию
                    findIndex()	- Индекс первого подходящего элемента
                    map() - Преобразует массив, возвращает новый
                    filter() - Возвращает новый массив с элементами, прошедшими проверку
                    reduce() - Сводит массив к одному значению
                    some() - Проверяет: есть ли хоть один элемент, подходящий. Возращает булевое значение.
                    every() - Проверяет: все ли элементы соответствуют условию. Возвращает булевое значение.
                    Array.from() - преобразует в массив массивоподобный объект, map, set, строки, 
                                 с объектом не работает
                    flat() - преобразует все вложенные массивы в один массив, аргументом 
                             указываем глубину вложености.
                    Array.isArray() - проверяет массив это или нет ,возращает либо true либо false.
11. Методы множества сет. 
                    const mySet = new Set(); создать пустой, или же передать аргументом массив, удалим дубли.
                    add(value) - добавить значение в множество.
                    delete(value) - удалить значение если есть.
                    has(value) - проверяет есть ли значение true или false.
                    clear() - удаляет все элементы.
                    size() - количество уникальных элементов(как length в массиве).
                    [...set] или Array.from(set) - преобразуем в массив.
12. Всплытие события. Если на родительский элементах есть событие, оно всплывает вверх от чилдрен к перент. 
    Например мы кликаем по чилдрен, выводим в консоль событие чилдрен, но если клик есть и на родителе
    выведет в консоль и его событие. Что бы отменить всплытие используем event.stopPropagation();
13. Погружение это обратный процесс, нажал по ребенку, сначала сработает событие родителя, потом уже 
    чилдрен.
14. Методы строк.  
                   .trim() - удаляет пробелы. 
                   .replace(searchValue, newValue) - заменяет вхождение в строке (первый аргумент), на 
                    другую часть указанную во втором аргументе.
                   .repeat(count) - повторяет строку , указанное число раз в аргументе. 
                    пример "ha".repeat(3) // "hahaha"
                   .split(separator) - строку преобразует в массив по разделителю в аргументе.
15. Методы объектов. 
                   Object.keys(obj) - возращает массив из ключей.
                      const obj = {a: 1, b: 2};
                      Object.keys(obj); // ["a", "b"]
                   
                   Object.values(obj) - возвращает массив из значений объекта. 
                   Object.entries(obj) - возращает массив пар ключ - значение.
                   Object.assign(target, ...sources) - копирует свойства из объекта, в целевой - первый
                       аргумент.
                       const target = {a: 1};
                       const source = {b: 2};
                       Object.assign(target, source);
                       console.log(target); // {a:1, b:2}

                   Object.hasOwnProperty(prop) - проверяет есть ли свойство в объекте (не унаследованное)
                   а именно в объкте.
                   
                   delete obj.prop - удалить свойство из объекта.
                   Object.freeze(obj) - делает объект не изменяемым нельзя добавлять изменять или удалять свойство.
                   
                   Object.seal(obj) - нельзя удалять , добавлять , но можно менять.
16. Как сравнить содержимое двух объектов a и b?? Ответ JSON.stringify(a) === JSON.stringify(b); 
17. Глубокое копирование объекта const deepCopy = JSON.parse(JSON.stringify(originalObj)); 
18. Глубокое копирование как примитивов, таки массивы, объекты, map, set.
const copy = structuredClone(originalObj); - новый метод.
19. В чем разница поверхностного от глубокого копирования. Ответ: поверхностное копирование 
копируют объект или массив, но внутренние объекты, массивы, все равно остаются с общей ссылкой
и при изменении их в копируемого объекте они поменятся и в оригинале. 
20. Чем отличается Function Declaration от Function Expression. Как работает поднятие. 
Ответ Function Declaration поднимается после создания( так как все объявления переменных, 
функций и т.д поднимаются а дальше снова браузер просматривает код.)Function Expression - поднимается
сама переменная, но не присвоение. 
21. Проверка типа a instanceof b - проверяет является ли значение а типом б.
22. Проверка свойство объекта с учетом прототипов. 'name' in tager - есть ли свойство нейм в объекте
тайгер.
23. Работа с числами:
               Number.isNaN(value) - фалс - если может быть преобразовано в число(пустая строка равно ноль), 
                       тру - если строка не может быть преобразовано в число.  
24. Что нужно делать для улучшения доступности в верстке? 